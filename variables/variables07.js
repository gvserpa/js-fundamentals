/*

Create a variable numberText with the value "42". Convert it to a number and add 8. Show the result in the console. Then, convert it back to a string.

*/

const numberText = "42";

const result = Number(numberText) + 8
console.log(result)

const resultAsString = String(result);
console.log(resultAsString)
