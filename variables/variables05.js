/* 

Create two variables: firstName and lastName. Then create a third variable called fullName that combines the two previous ones. Display it in the console.

*/

const name = 'Gustavo';
const lastName = 'Vieira Serpa';

const fullName = `${name} ${lastName}`

console.log(fullName)