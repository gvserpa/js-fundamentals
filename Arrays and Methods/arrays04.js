/*

Remove the first and last item from the fruits array using shift() and pop(). Display the array after each removal.

*/

const fruits = ['orange', 'apple', 'banana', 'watermelon'];

fruits.shift()
console.log(fruits)

fruits.pop()
console.log(fruits)