/* 

Add a new fruit to the end of the array using push() and one to the beginning using unshift(). Display the array after each addition.

*/

const fruits = ['orange', 'apple', 'banana', 'watermelon'];

fruits.push('Peach')
console.log(fruits)

fruits.unshift('Melons')
console.log(fruits)