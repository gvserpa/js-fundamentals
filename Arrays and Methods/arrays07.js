/* 

Create a copy of the first two elements of the fruits array using slice(). Display this new copy in the console.

*/

const fruits = ['orange', 'apple', 'banana', 'watermelon'];

console.log(fruits.slice(0, 2))