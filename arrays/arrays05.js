/* 

Check if the fruit "banana" is in the array using includes(). If it is, display: "There is banana!", otherwise, display: "No banana here."

*/

const fruits = ['orange', 'apple', 'banana', 'watermelon'];

if (fruits.includes('banana')) {
    console.log('There is banana!')
} else {
    console.log('No banana here.')
}

